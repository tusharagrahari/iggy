# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.

name: csharp-dotnet-pre-merge
description: .NET pre-merge testing github iggy actions

inputs:
  task:
    description: "Task to run (lint, test, build, e2e-<tfm>)"
    required: true

runs:
  using: "composite"
  steps:
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: |
          8.0.x
          10.0.x

    - name: Setup Rust with cache
      if: inputs.task == 'test' || startsWith(inputs.task, 'e2e')
      uses: ./.github/actions/utils/setup-rust-with-cache

    - name: Restore dependencies
      run: |
        dotnet restore foreign/csharp/Iggy_SDK.sln
        dotnet restore examples/csharp/Iggy_SDK.Examples.sln
      shell: bash

    - name: Build
      run: |
        dotnet build foreign/csharp/Iggy_SDK.sln --no-restore
        dotnet build examples/csharp/Iggy_SDK.Examples.sln --no-restore
      shell: bash

    - name: Lint (Code Analysis)
      if: inputs.task == 'lint'
      run: |
        # Run code analysis
        dotnet build foreign/csharp/Iggy_SDK.sln --no-restore /p:EnforceCodeStyleInBuild=true /p:TreatWarningsAsErrors=false
        dotnet build examples/csharp/Iggy_SDK.Examples.sln --no-restore /p:EnforceCodeStyleInBuild=true /p:TreatWarningsAsErrors=false

        dotnet format examples/csharp/Iggy_SDK.Examples.sln --verify-no-changes --verbosity diagnostic
        dotnet format foreign/csharp/Iggy_SDK.sln --verify-no-changes --verbosity diagnostic

      shell: bash

    - name: Test
      if: inputs.task == 'test'
      working-directory: foreign/csharp
      run: |
        # Run unit tests
        dotnet test --project Iggy_SDK_Tests \
          --no-build \
          --verbosity normal \
          --coverage \
          --coverage-output-format cobertura \
          --coverage-output coverage.cobertura.xml \
          --results-directory ./reports

      shell: bash

    - name: Build Iggy server Docker image
      id: docker_build
      if: startsWith(inputs.task, 'e2e')
      shell: bash
      run: |
        cargo build --locked --bin iggy-server --bin iggy
        docker build \
          -f core/server/Dockerfile \
          --target runtime-prebuilt \
          -t iggy-server:test \
          --build-arg PREBUILT_IGGY_SERVER=target/debug/iggy-server \
          --build-arg PREBUILT_IGGY_CLI=target/debug/iggy \
          .
        echo "docker_image=iggy-server:test" >> "$GITHUB_OUTPUT"

    - name: Run integration tests
      if: startsWith(inputs.task, 'e2e')
      working-directory: foreign/csharp
      env:
        IGGY_SERVER_DOCKER_IMAGE: ${{ steps.docker_build.outputs.docker_image }}
        IGGY_TEST_LOGS_DIR: ./reports/container-logs
        IGGY_TEST_CLUSTER_NODES: "3"
        IGGY_TASK: ${{ inputs.task }}
      run: |
        # Task name carries the target framework: e2e-net8.0 -> net8.0
        dotnet test --project Iggy_SDK.Tests.Integration \
          --framework "${IGGY_TASK#e2e-}" \
          --no-build \
          --verbosity normal \
          --coverage \
          --coverage-output-format cobertura \
          --coverage-output coverage.cobertura.xml \
          --results-directory ./reports \
          -- --report-trx --retry-failed-tests 3
      shell: bash

    - name: Collect container logs
      if: startsWith(inputs.task, 'e2e') && always()
      shell: bash
      run: |
        mkdir -p foreign/csharp/reports/container-logs
        find foreign/csharp/Iggy_SDK.Tests.Integration/bin -name "*.log" -path "*/container-logs/*" -exec cp {} foreign/csharp/reports/container-logs/ \; || true

    - name: Upload Test Results
      uses: actions/upload-artifact@v7
      if: startsWith(inputs.task, 'e2e') && always()
      with:
        name: dotnet-test-results-${{ inputs.task }}
        path: foreign/csharp/reports
        retention-days: 7

    - name: Build Release
      if: inputs.task == 'build'
      working-directory: foreign/csharp
      run: |
        # Build in Release mode
        dotnet build Iggy_SDK.sln -c Release --no-restore

        # List build output
        echo "Build output:"
        find . -name "*.dll" -path "Iggy_SDK/bin/Release/*" | head -20 || echo "No DLLs found in Release folders"
      shell: bash
